import 'package:flutter/material.dart';

class VideoChewie extends StatelessWidget {
  const VideoChewie({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}